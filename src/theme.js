import { createTheme } from "@mui/system";

export let theme = createTheme({
  palette: {
    primary: {
      main: "#007bff",
      secondary:'#b2beb5',
      contrastThreshold: 3.1,
    },
  },
});
